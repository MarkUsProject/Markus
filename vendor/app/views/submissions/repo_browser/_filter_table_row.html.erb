<td>
  <%=image_tag('icons/page_white_text.png') %>
  <%= link_to h(file.name), :action => 'download', :id => @assignment.id, :revision_number => @revision_number, :file_name => file.name, :path => @path, :grouping_id => @grouping.id %>
</td>
<td>
  <%=h I18n.l(file.last_modified_date, :format => :long_date) %>
</td>
<td>
  <%= h(file.user_id) %>
</td>
