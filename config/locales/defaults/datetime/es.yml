---
es:
  date:
    before_now_warning: 'PRECAUCIÓN: La fecha seleccionada está en el pasado.'
    format_string:
      datetimepicker: yy-mm-dd
      default: DD, MM d, yy
      placeholder: YYYY-MM-DD
  durations:
    any_hours: hora(s)
    any_minutes: minuto(s)
    days:
      one: día
      other: días
    hours:
      one: hora
      other: horas
    minutes:
      one: minuto
      other: minutos
    weeks:
      one: semana
      other: semanas
  time:
    format_string:
      js: YYYY-MM-DD h:mm A
      placeholder: YYYY-MM-DD H:MM AM/PM
    formats:
      default: "%A, %B %d, %Y, %l:%M:%S %p %Z"
    next: Siquiente
    prev: Previo
