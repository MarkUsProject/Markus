---
en:
  activerecord:
    attributes:
      section:
        name: Name
    errors:
      models:
        section:
          attributes:
            name:
              invalid: "%{attribute} must be alphanumeric, hyphen, whitespace, or underscore characters"
    models:
      section:
        one: Section
        other: Sections
