---
en:
  activerecord:
    attributes:
      checkbox_criterion:
        description: Description
        max_mark: Maximum mark
        name: Name
        peer_visible: Make visible to peer reviewers
        ta_visible: Make visible to graders and admins
      criterion:
        bonus: Bonus
        description: Description
        max_mark: Maximum mark
        name: Name
        peer_visible: Make visible to peer reviewers
        ta_visible: Make visible to graders and admins
      flexible_criterion:
        description: Description
        max_mark: Maximum mark
        name: Name
        peer_visible: Make visible to peer reviewers
        ta_visible: Make visible to graders and admins
      rubric_criterion:
        description: Description
        max_mark: Maximum mark
        name: Name
        peer_visible: Make visible to peer reviewers
        ta_visible: Make visible to graders and admins
    errors:
      models:
        criterion:
          visibility_error: Select at least one box in the visibility section
    models:
      checkbox_criterion:
        one: Checkbox Criterion
      criterion:
        one: Criterion
        other: Criteria
      flexible_criterion:
        one: Flexible Criterion
      rubric_criterion:
        one: Rubric Criterion
