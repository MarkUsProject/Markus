---
en:
  activerecord:
    attributes:
      assessment:
        max_mark: Out Of
    errors:
      models:
        assessment:
          attributes:
            short_identifier:
              format: "%{attribute} must only contain alphanumeric, hyphen, or underscore characters"
    models:
      assessment:
        one: Assessment
        other: Assessments
