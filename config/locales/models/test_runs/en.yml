---
en:
  activerecord:
    attributes:
      test_run:
        status: Status
        user: Run by
    errors:
      models:
        test_run:
          invalid_test_id: autotest_test_id must be unique scoped to autotest settings
    models:
      test_run:
        one: Test Run
        other: Test Runs
