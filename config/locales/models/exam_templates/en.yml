---
en:
  activerecord:
    attributes:
      exam_template:
        cover_fields: Field to match by
        filename: Filename
        name: Name
        num_pages: pages
        template_divisions: Template Divisions
    errors:
      models:
        exam_template:
          attributes:
            name:
              too_long: must be at most %{count} characters
    models:
      exam_template:
        one: Exam Template
        other: Exam Templates
