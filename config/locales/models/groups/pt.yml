---
pt:
  activerecord:
    attributes:
      group:
        group_name: Nome do grupo
        student_memberships: Membros
    errors:
      models:
        group:
          attributes:
            repo_name:
              taken: Repositório %{value} já existe.
    models:
      group:
        one: Grupo
