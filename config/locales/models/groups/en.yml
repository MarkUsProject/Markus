---
en:
  activerecord:
    attributes:
      group:
        group_name: Group name
        student_memberships: Members
    errors:
      models:
        group:
          attributes:
            group_name:
              invalid: "%{attribute} must only contain alphanumeric, hyphen, a blank space, or underscore characters"
            repo_name:
              invalid: "%{attribute} must only contain alphanumeric, hyphen, a blank space, or underscore characters"
              taken: Repository %{value} already exists.
    models:
      group:
        one: Group
        other: Groups
