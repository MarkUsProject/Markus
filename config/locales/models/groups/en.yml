---
en:
  activerecord:
    attributes:
      group:
        group_name: Group name
        student_memberships: Members
    errors:
      models:
        group:
          attributes:
            repo_name:
              taken: Repository %{value} already exists.
              format: must only contain alphanumeric, hyphen, a blank space, or underscore
            group_name:
              format: must only contain alphanumeric, hyphen, a blank space, or underscore
    models:
      group:
        one: Group
        other: Groups
