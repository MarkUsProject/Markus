---
en:
  activerecord:
    attributes:
      course:
        display_name: Display Name
        is_hidden: Course Visibility
        max_file_size: Maximum File Size (bytes)
        name: Name
    errors:
      models:
        course:
          attributes:
            name:
              format: name must only contain alphanumeric, hyphen, or underscore
    models:
      course:
        one: Course
        other: Courses
