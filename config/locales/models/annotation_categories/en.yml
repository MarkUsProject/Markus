---
en:
  activerecord:
    attributes:
      annotation_category:
        annotation_category_name: Category name
        flexible_criterion: Associated Criterion (Flexible type only)
    errors:
      models:
        annotation_category:
          attributes:
            flexible_criterion_id:
              inclusion: "%{value} is an invalid criterion for this assignment."
          cannot_delete_annotation: Cannot delete annotation category once deductions have been applied
          cannot_update_flex: Cannot update annotation category flexible criterion once results are released
    models:
      annotation_category:
        one: Annotation Category
        other: Annotation Categories
