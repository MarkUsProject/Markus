<h1>Manage <%= params[:role] %>s</h1>

<div class="colsLeftHeavy">

  <div class="colRight">
  
  <h2>Register <%= params[:role] %>s</h2>
  
    <h3>Upload a class list</h3>
    
    <div class="section">
      
      <p>Select a CSV file of the following form: <code>user_name, student_number, last_name, first_name</code></p>
      
      <p>If a <%= params[:role] %> with the same student number exists, then the <%= params[:role] %>'s information is updated.</p>
      
      <%= form_tag "/checkmark/users/#{params[:role]}/update_userlist", :multipart => true %>
        <label for="userlist">CVS File</label>
        <input type="file" name="userlist" id="userlist"/>
        <%= submit_tag "Update userlist", :disable_with => 'Processing...', :class => 'button' %>
      </form>
    
    </div>
    
    <h3>Add Each <%= params[:role] %> Individually</h3>
    
    <div class="section">
      <p><%= link_to "Add a new #{params[:role]}", :controller => 'users', :action => 'create' %></p>
    </div>
    
  </div>
  
  <div class="colLeft"><div class="wrapLeft">
  
    <h2>View <%= params[:role] %> list (<%= link_to "XML", :action => 'classlist', :format => 'xml' %>)</h2>
    
      <% if flash[:upload_notice] -%>
          <p class="success"><%=h flash[:upload_notice] %></p>
      <% end -%>
      
      <% if flash[:edit_notice] -%>
        <p style="color:green"><%=h flash[:edit_notice] %></p>
      <% end -%>
      
      <% # display lines in csv file that has not been successfully processed %>
      <% if flash[:invalid_lines] && !flash[:invalid_lines].empty? -%>
      <div class="errorExplanation" id="errorExplanation">
        <p>The following <%= params[:role] %>(s) have not been successfully added:</p>
        <ul>
          <% for line in flash[:invalid_lines] -%>
          <li><%=h line %></li>
          <% end -%>
        </ul>
        <p>Make sure that:</p>
        <ul>
          <li>Student numbers are 9 digits</li>
          <li>Student names are alphanumeric</li>
          <li>Each field is separated by a comma (no spaces)</li>
          <li>Each line has the required fields specified above</li>
        </ul>
      </div>
      <% end -%>
      
      <table>
        
       <tr>
          <th>CDF id</th>
          <th>Student Number</th>
          <th>Last Name</th>
          <th>First Name</th>
          <th></th>
        </tr>
          
        <% for u in @users %>
          <tr class="<%= cycle('rowA', 'rowB') %>">
            <td><%=h u.user_name %></td>
            <td><%=h u.user_number %></td>
            <td><%=h u.last_name %></td>
            <td><%=h u.first_name %></td>
            <td><%= link_to 'Edit', :controller => 'users', :action => 'edit', :role => u.role, :id => u %></td>
          </tr>
        <% end %>
        
      </table>
  
  </div></div>

</div> <!-- colsLeftHeavy -->
