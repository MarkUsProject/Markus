<%= content_for :modal_id, 'download_dialog' %>
<%= content_for :modal_title, t('groups.download.download_grader_maps') %>
<%= content_for :modal_open_link, '#downloadModal' %>

<%= content_for :modal_content do %>

  <div class='clickable_links'>
    <div class='clickable_text'>
      <img class='clickable_image' src='<%= image_path('icons/download-file.png') %>'>
      <%= t('groups.download.download_grader_groups_csv') %>
    </div>
    <%= link_to '',
                { action: 'download_grader_groupings_mapping',
                  id: @graders,
                  format: 'csv' },
                class: 'make_div_clickable' %>
  </div>
  <div class='clickable_links'>
    <div class='clickable_text'>
      <img class='clickable_image' src='<%= image_path('icons/download-file.png') %>'>
      <%= t('groups.download.download_grader_criteria_csv') %>
    </div>
    <%= link_to '',
                { action: 'download_grader_criteria_mapping',
                  id: @graders,
                  format: 'yml' },
                class: 'make_div_clickable' %>
  </div>
  <section class='dialog-actions'>
    <input type='reset' id='download' value='<%= t(:cancel) %>'/>
  </section>
<% end %>
