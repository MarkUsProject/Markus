<h2><%= t('student.submission.conflicts') %></h2>
<p><%= t('student.submission.file_conflicts') %></p>
<ul>
  <% @file_manager_errors[:update_conflicts].each do |conflict| %>
    <li><%= conflict.to_s %></li>
  <% end %>
</ul>
