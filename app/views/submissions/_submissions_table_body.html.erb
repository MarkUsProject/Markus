<tbody id="submissions">
  <% groupings.each do |grouping| %>
    <%= render :partial => 'submissions/submissions_table_row/table_row', :locals => {:grouping => grouping, :assignment => assignment, :per_page => @per_page} %>
  <% end %>
<tbody>
