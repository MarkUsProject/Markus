if (!<%= success %>) {
	jQuery("#role_switch_error").html('<%= escape_javascript(render(:partial => "role_switch_error.html.erb", :locals => { :error => error })) %>');
} else {
	window.location.replace("<%= assignments_path() %>");
}