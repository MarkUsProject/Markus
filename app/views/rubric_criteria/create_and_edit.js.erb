jQuery('#rubric_criteria_pane_list').append("<%= escape_javascript(render partial: 'rubric_criterion',
                                                                          locals: { criterion: @criterion }) %>");

jQuery('#new_rubric_criterion').remove();

document.getElementById('rubric_levels_pane').innerHTML =
  "<%= escape_javascript(render partial: 'criterion_editor',
                                locals: { criterion: @criterion }) %>";

document.getElementById('rubric_total_weight').innerHTML = "<%= @criterion.assignment.total_criteria_weight %>";
