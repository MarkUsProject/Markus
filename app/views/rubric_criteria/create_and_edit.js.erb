jQuery('#criteria_pane_list').append("<%= escape_javascript(render partial: 'criteria/criterion',
                                                                          locals: { criterion: @criterion }) %>");

jQuery('#new_rubric_criterion').remove();

document.getElementById('rubric_levels_pane').innerHTML =
  "<%= escape_javascript(render partial: 'criterion_editor',
                                locals: { criterion: @criterion }) %>";

document.getElementById('criteria_total_max_mark').innerHTML = "<%= @criterion.assignment.max_mark %>";
