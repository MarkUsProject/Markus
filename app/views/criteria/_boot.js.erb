<script>
  $(document).ready(function() {
    bindSortable();

    var ajaxRequest;
    function bindSortable() {
      $('.sortable ul').sortable({
        axis:   'y',
        cursor: 'move',
        update: function() {
          // Cancel any previous requests
          if (typeof ajaxRequest !== 'undefined') {
            ajaxRequest.abort();
          }

          ajaxRequest = $.ajax({
            url:  '<%= update_positions_assignment_criteria_path(@assignment) %>',
            type: 'POST',
            data: $('.sortable ul').sortable('serialize'),
            dataType: 'script',
            success: function(response) {
              ajaxRequest = undefined;
              bindSortable();
            }
          });
        },
        cancel: '#new_criterion'
      });
    }
  });
  function add_level(id) {
    var rubric_form = $('#' + id);
    var last_level = $('#' + id + " .level").first();
    var new_level = last_level.clone();

    var regex = new RegExp(/\d+/);
    var level_num = $('#' + id + " .level").length;

    var new_name = $(new_level)[0].childNodes[1];
    $(new_name).text(new_name.textContent.replace(regex, level_num));

    var display_name = new_level.find('label')[0];
    $(display_name).text(display_name.textContent.replace(regex, level_num));


    // $(new_level + " .rubric_level_header").val('wooohooo');

    new_level.find('input').each(function () {
      if ($(this).attr('id')) {
        $(this).attr('id', $(this).attr('id').replace(regex, level_num));
      }
      if ($(this).attr('name')) {
        $(this).attr('name',
                          $(this).attr('name').replace(regex, level_num));
      }
      $(this).val('');
    });
    new_level.find('textarea').each(function () {
      $(this).val('');
    });

    new_level.find('textarea').each(function () {
      $(this).val('');
    });

    rubric_form.prepend(new_level);
  
    // rubric_form.refresh();
  }
</script>
