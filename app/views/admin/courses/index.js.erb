document.getElementById('editing_pane_menu').innerHTML =
  "<%= escape_javascript(render partial: "/admin/main_admin/courses_list") %>";

const api_key = "<%= @current_user.api_key.nil? ? @current_user.reset_api_key : @current_user.api_key %>"
makeAdminCourseList(document.getElementById('admin_courses_list'), { apiKey: api_key });
