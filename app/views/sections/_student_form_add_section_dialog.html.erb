<!-- Modal Window -->
<%= form_for(@section, remote: true) do |f| %>
  <div id='add_section_error'>
    <span>&nbsp;</span>
  </div>

  <h2><%= t('section.add_section') %></h2>

  <div>
    <%= raw(f.label :name, t('section.name')) %>
    <%= raw(f.text_field :name) %>
  </div>

  <div>
    <%= hidden_field_tag 'section_modal', 'true' %>
    <%= f.submit t(:save) , data: { disable_with: t('working') } %>
    <%= button_to_function t(:close), 'modal_add_section.close()' %>
  </div>
<% end %>
