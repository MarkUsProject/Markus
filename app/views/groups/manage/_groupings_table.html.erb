<% groupings.each do |grouping| -%>
  <tr id="grouping_<%= grouping.id %>_row">
  <%= render :partial => "groups/manage/group", :locals =>
    {:grouping => grouping, :assignment => assignment} %> 
  </tr>
<% end -%>
