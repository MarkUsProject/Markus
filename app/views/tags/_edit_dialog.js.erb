$('#edit_tags_dialog').html("<%= escape_javascript(render('tags/edit')) %>");
modal_edit.open();
$(function() {
  $('#edit_tags_dialog :reset').click(function(e) {
    modal_edit.close();
    e.preventDefault();
  })
})
