<%= content_for :modal_id, 'download_dialog' %>
<%= content_for :modal_title, t('download') %>
<%= content_for :modal_content do %>

  <div class='clickable_links'>
    <div class='clickable_text'>
      <img class='clickable_image' src='<%= image_url('icons/download-file.png') %>'>
      <%=t('graders.download.download_list_csv') %>
    </div>
    <%= link_to '',
                { action: 'download_ta_list',
                  id: @tags,
                  format: 'csv' },
                onclick: 'jQuery(this).closest(".dialog").trigger("closeModal");',
                class: 'make_div_clickable' %>
  </div>
  <div class='clickable_links'>
    <div class='clickable_text'>
      <img class='clickable_image' src='<%= image_url('icons/download-file.png') %>'>
      <%=t('graders.download.download_list_yml') %>
    </div>
    <%= link_to '',
                { action: 'download_ta_list',
                  id: @tags,
                  format: 'yml' },
                onclick: 'jQuery(this).closest(".dialog").trigger("closeModal");',
                class: 'make_div_clickable' %>
  </div>
  <section class='dialog-actions'>
    <input type='reset' id='download' value='<%= t(:cancel) %>' onclick='jQuery(this).closest(".dialog").trigger("closeModal");'/>
  </section>
<% end %>
