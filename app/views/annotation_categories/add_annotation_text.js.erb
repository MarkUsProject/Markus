jQuery('#annotation_list').prepend(
  "<%= escape_javascript("#{render partial: 'new_annotation_text',
                                   locals: { annotation_category_id: @annotation_category.id }}").html_safe %>");
jQuery('#annotation_text_content').select();