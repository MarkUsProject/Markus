<div class='title_bar'>
  <h1><%= t('grade_assignments') %></h1>
</div>

<div class='wrapper'>
  <%= render partial: 'assignments/list_manage',
             locals: { controller: 'submissions',
                       action: 'browse' } %>

  <%= render partial: 'grade_entry_forms/list_manage',
             locals: { action: 'grades' } %>
</div>
