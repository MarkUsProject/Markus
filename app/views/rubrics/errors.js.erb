document.getElementById('criterion_<%= @criterion.id %>_error').innerHTML =
  "<%= escape_javascript(render partial: 'errors_list',
                                locals: { criterion: @criterion }) %>";

document.getElementById('criterion_<%= @criterion.id %>_error').style.display = '';
