jQuery('#rubric_criteria_pane_list').prepend(
  "<%= escape_javascript(render partial: 'new_rubric_criterion',
                                locals: { assignment: @assignment }).html_safe %>");

document.getElementById('new_rubric_criterion_prompt').focus();
