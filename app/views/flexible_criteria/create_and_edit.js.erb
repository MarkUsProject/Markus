jQuery('#flexible_criteria_pane_list').append("<%= escape_javascript(render partial: 'flexible_criterion',
                                                                            locals: { criterion: @criterion }) %>")

jQuery('#new_flexible_criterion').remove();

document.getElementById('flexible_edition_pane_menu').innerHTML =
  "<%= escape_javascript(render partial: 'criterion_editor',
                                locals: { criterion: @criterion }) %>"

document.getElementById('criteria_total_mark').innerHTML = "<%= @criterion.assignment.total_mark %>";
