$('#add_new_section_dialog').html("<%= escape_javascript(render partial: 'sections/student_form_add_section_dialog').html_safe %>");
modal_add_section.open();

$('#modal-add-section-close').click(() => {modal_add_section.close()})
