poll_job("<%= session[:job_id] %>", undefined, function() {
    console.log("test");
    var s = "window.location='<%= download_generate_assignment_exam_template_path(file_name: file_name,
                                                                         id: exam_id) %>'";
    eval(s);
});
