<% content_for :head do %>
    <%= render partial: 'boot',
               formats: [:js],
               handlers: [:erb] %>
<% end %>

<% content_for :title, ExamTemplate.model_name.human.pluralize %>

<% @heading_buttons = [
    { link_text: 'Add Exam Template',
      link_path: '#',
      html_options: { onclick:'modal_create_new.open()' } }
] %>

<!-- Modals -->
<%= render partial: 'create_new_template', layout: 'layouts/modal_dialog'  %>

<%= render partial: 'form',
           locals: { exam_template: @exam_templates[0] }%>
