<div class='listings sortable'>
  <header>
    <span><%= ExamTemplate.model_name.human %></span>
  </header>

  <ul id='exam_template_pane_list'>
    <% @exam_templates.each do |exam_template| %>
      <%= render partial: 'exam_template',
                 locals: { exam_template: exam_template } %>
    <% end %>
  </ul>
</div>
