<% if session[:job_id] %>
  <% job_status = ActiveJob::Status.get(session[:job_id]) %>
  poll_job("<%= session[:job_id] %>", undefined, function(){<%= raw job_status[:job_class].on_complete_js(job_status) %>});
<% end %>
