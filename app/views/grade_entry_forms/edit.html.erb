<% # Grade entry form edit properties page
%>

<div id="title_bar">
  <h1><%= t('grade_entry_forms.edit.title') + " " + h(@grade_entry_form.short_identifier) %></h1>
  <p><%= t("assignment.required_fields").html_safe %></p>
</div>
<%= render :partial => 'form' %>
